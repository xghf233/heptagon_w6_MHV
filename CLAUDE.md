# Claude Code Instructions

Read and follow `AGENTS.md` before taking any action in this repository.

This is a local code-editing sandbox. Edit locally, but do not run tests, smoke
tests, training, inference, package installation, or downloads on the Mac. All
execution happens later on AutoDL after the user reviews the local diff and
explicitly approves the remote command.
